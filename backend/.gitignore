# Virtual environment
venv/
env/

# Environment variables
.env

# Data files (pickle files are large, regenerate locally)
data/*.pkl

# Note: embeddings.npy is now committed (15KB, needed for Railway deployment)

# But keep JSON documents (small, needed for setup)
!data/*.json
!data/metadata.json

# Python cache
__pycache__/
*.pyc