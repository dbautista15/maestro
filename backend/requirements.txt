# API Framework
fastapi
uvicorn[standard]

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
requests

# Caching
langcache

# Google AI
google-generativeai

# PyTorch CPU-only (install BEFORE sentence-transformers to avoid GPU version)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.0+cpu
torchvision==0.17.0+cpu

# Embeddings (will use CPU torch from above)
sentence-transformers>=2.2.0
huggingface-hub>=0.19.0

# Data Processing
numpy<2.0  # Pin to 1.x for PyTorch compatibility
scipy

# Environment & Config
python-dotenv

# Utilities
python-multipart
